{
  "resourceType": "ConceptMap",
  "id": "R5-DetectedIssue-element-map-to-R4",
  "url": "http://hl7.org/fhir/uv/xver/ConceptMap/R5-DetectedIssue-element-map-to-R4",
  "version": "0.1.1",
  "name": "R5DetectedIssueElementMapToR4",
  "title": "Cross-version mapping for FHIR R5 DetectedIssue to FHIR R4 DetectedIssue",
  "status": "active",
  "experimental": false,
  "date": "2026-06-12T16:41:43.6830653-05:00",
  "description": "This ConceptMap represents cross-version mappings for elements from a FHIR R5 DetectedIssue to FHIR R4.",
  "sourceCanonical": "http://hl7.org/fhir/5.0",
  "targetUri": "http://hl7.org/fhir/4.0",
  "group": [
    {
      "source": "http://hl7.org/fhir/StructureDefinition/DetectedIssue",
      "sourceVersion": "5.0.0",
      "target": "http://hl7.org/fhir/StructureDefinition/DetectedIssue",
      "targetVersion": "4.0.1",
      "element": [
        {
          "code": "DetectedIssue.meta",
          "display": "meta",
          "target": [
            {
              "code": "DetectedIssue.meta",
              "display": "DetectedIssue.meta",
              "equivalence": "equivalent",
              "comment": "Element `DetectedIssue.meta` is mapped to FHIR R4 element `DetectedIssue.meta` as `Equivalent`."
            },
            {
              "code": "DetectedIssue",
              "display": "DetectedIssue",
              "equivalence": "equivalent",
              "comment": "Element `DetectedIssue.meta` is mapped to FHIR R4 element `DetectedIssue.meta` as `Equivalent`."
            }
          ]
        },
        {
          "code": "DetectedIssue.implicitRules",
          "display": "implicitRules",
          "target": [
            {
              "code": "DetectedIssue.implicitRules",
              "display": "DetectedIssue.implicitRules",
              "equivalence": "equivalent",
              "comment": "Element `DetectedIssue.implicitRules` is mapped to FHIR R4 element `DetectedIssue.implicitRules` as `Equivalent`.\nAsserting this rule set restricts the content to be only understood by a limited set of trading partners. This inherently limits the usefulness of the data in the long term. However, the existing health eco-system is highly fractured, and not yet ready to define, collect, and exchange data in a generally computable sense. Wherever possible, implementers and/or specification writers should avoid using this element. Often, when used, the URL is a reference to an implementation guide that defines these special rules as part of its narrative along with other profiles, value sets, etc."
            },
            {
              "code": "DetectedIssue",
              "display": "DetectedIssue",
              "equivalence": "equivalent",
              "comment": "Element `DetectedIssue.implicitRules` is mapped to FHIR R4 element `DetectedIssue.implicitRules` as `Equivalent`.\nAsserting this rule set restricts the content to be only understood by a limited set of trading partners. This inherently limits the usefulness of the data in the long term. However, the existing health eco-system is highly fractured, and not yet ready to define, collect, and exchange data in a generally computable sense. Wherever possible, implementers and/or specification writers should avoid using this element. Often, when used, the URL is a reference to an implementation guide that defines these special rules as part of its narrative along with other profiles, value sets, etc."
            }
          ]
        },
        {
          "code": "DetectedIssue.language",
          "display": "language",
          "target": [
            {
              "code": "DetectedIssue.language",
              "display": "DetectedIssue.language",
              "equivalence": "equivalent",
              "comment": "Element `DetectedIssue.language` is mapped to FHIR R4 element `DetectedIssue.language` as `Equivalent`.\nLanguage is provided to support indexing and accessibility (typically, services such as text to speech use the language tag). The html language tag in the narrative applies  to the narrative. The language tag on the resource may be used to specify the language of other presentations generated from the data in the resource. Not all the content has to be in the base language. The Resource.language should not be assumed to apply to the narrative automatically. If a language is specified, it should it also be specified on the div element in the html (see rules in HTML5 for information about the relationship between xml:lang and the html lang attribute)."
            },
            {
              "code": "DetectedIssue",
              "display": "DetectedIssue",
              "equivalence": "equivalent",
              "comment": "Element `DetectedIssue.language` is mapped to FHIR R4 element `DetectedIssue.language` as `Equivalent`.\nLanguage is provided to support indexing and accessibility (typically, services such as text to speech use the language tag). The html language tag in the narrative applies  to the narrative. The language tag on the resource may be used to specify the language of other presentations generated from the data in the resource. Not all the content has to be in the base language. The Resource.language should not be assumed to apply to the narrative automatically. If a language is specified, it should it also be specified on the div element in the html (see rules in HTML5 for information about the relationship between xml:lang and the html lang attribute)."
            }
          ]
        },
        {
          "code": "DetectedIssue.text",
          "display": "text",
          "target": [
            {
              "code": "DetectedIssue.text",
              "display": "DetectedIssue.text",
              "equivalence": "equivalent",
              "comment": "Element `DetectedIssue.text` is mapped to FHIR R4 element `DetectedIssue.text` as `Equivalent`.\nContained resources do not have a narrative. Resources that are not contained SHOULD have a narrative. In some cases, a resource may only have text with little or no additional discrete data (as long as all minOccurs=1 elements are satisfied).  This may be necessary for data from legacy systems where information is captured as a \"text blob\" or where text is additionally entered raw or narrated and encoded information is added later."
            },
            {
              "code": "DetectedIssue",
              "display": "DetectedIssue",
              "equivalence": "equivalent",
              "comment": "Element `DetectedIssue.text` is mapped to FHIR R4 element `DetectedIssue.text` as `Equivalent`.\nContained resources do not have a narrative. Resources that are not contained SHOULD have a narrative. In some cases, a resource may only have text with little or no additional discrete data (as long as all minOccurs=1 elements are satisfied).  This may be necessary for data from legacy systems where information is captured as a \"text blob\" or where text is additionally entered raw or narrated and encoded information is added later."
            }
          ]
        },
        {
          "code": "DetectedIssue.contained",
          "display": "contained",
          "target": [
            {
              "code": "DetectedIssue.contained",
              "display": "DetectedIssue.contained",
              "equivalence": "equivalent",
              "comment": "Element `DetectedIssue.contained` is mapped to FHIR R4 element `DetectedIssue.contained` as `Equivalent`.\nThis should never be done when the content can be identified properly, as once identification is lost, it is extremely difficult (and context dependent) to restore it again. Contained resources may have profiles and tags in their meta elements, but SHALL NOT have security labels."
            },
            {
              "code": "DetectedIssue",
              "display": "DetectedIssue",
              "equivalence": "equivalent",
              "comment": "Element `DetectedIssue.contained` is mapped to FHIR R4 element `DetectedIssue.contained` as `Equivalent`.\nThis should never be done when the content can be identified properly, as once identification is lost, it is extremely difficult (and context dependent) to restore it again. Contained resources may have profiles and tags in their meta elements, but SHALL NOT have security labels."
            }
          ]
        },
        {
          "code": "DetectedIssue.identifier",
          "display": "identifier",
          "target": [
            {
              "code": "DetectedIssue.identifier",
              "display": "DetectedIssue.identifier",
              "equivalence": "equivalent",
              "comment": "Element `DetectedIssue.identifier` is mapped to FHIR R4 element `DetectedIssue.identifier` as `Equivalent`."
            },
            {
              "code": "DetectedIssue",
              "display": "DetectedIssue",
              "equivalence": "equivalent",
              "comment": "Element `DetectedIssue.identifier` is mapped to FHIR R4 element `DetectedIssue.identifier` as `Equivalent`."
            }
          ]
        },
        {
          "code": "DetectedIssue.status",
          "display": "status",
          "target": [
            {
              "code": "DetectedIssue.status",
              "display": "DetectedIssue.status",
              "equivalence": "wider",
              "comment": "Element `DetectedIssue.status` is mapped to FHIR R4 element `DetectedIssue.status` as `RelatedTo`.\nThe mappings for `DetectedIssue.status` do not allow expression of the necessary codes, per the bindings on the source and target.\nThe target context `DetectedIssue.status` is a modifier element, so this extension does not need to be defined as a modifier.\nThis element is labeled as a modifier because the status contains the code entered-in-error that marks the issue as not currently valid."
            },
            {
              "code": "DetectedIssue",
              "display": "DetectedIssue",
              "equivalence": "wider",
              "comment": "Element `DetectedIssue.status` is mapped to FHIR R4 element `DetectedIssue.status` as `RelatedTo`.\nThe mappings for `DetectedIssue.status` do not allow expression of the necessary codes, per the bindings on the source and target.\nThe target context `DetectedIssue.status` is a modifier element, so this extension does not need to be defined as a modifier.\nThis element is labeled as a modifier because the status contains the code entered-in-error that marks the issue as not currently valid."
            }
          ]
        },
        {
          "code": "DetectedIssue.code",
          "display": "code",
          "target": [
            {
              "code": "DetectedIssue.code",
              "display": "DetectedIssue.code",
              "equivalence": "equivalent",
              "comment": "Element `DetectedIssue.code` is mapped to FHIR R4 element `DetectedIssue.code` as `Equivalent`."
            },
            {
              "code": "DetectedIssue",
              "display": "DetectedIssue",
              "equivalence": "equivalent",
              "comment": "Element `DetectedIssue.code` is mapped to FHIR R4 element `DetectedIssue.code` as `Equivalent`."
            }
          ]
        },
        {
          "code": "DetectedIssue.severity",
          "display": "severity",
          "target": [
            {
              "code": "DetectedIssue.severity",
              "display": "DetectedIssue.severity",
              "equivalence": "equivalent",
              "comment": "Element `DetectedIssue.severity` is mapped to FHIR R4 element `DetectedIssue.severity` as `Equivalent`."
            },
            {
              "code": "DetectedIssue",
              "display": "DetectedIssue",
              "equivalence": "equivalent",
              "comment": "Element `DetectedIssue.severity` is mapped to FHIR R4 element `DetectedIssue.severity` as `Equivalent`."
            }
          ]
        },
        {
          "code": "DetectedIssue.subject",
          "display": "subject",
          "target": [
            {
              "code": "DetectedIssue.patient",
              "display": "DetectedIssue.patient",
              "equivalence": "wider",
              "comment": "Element `DetectedIssue.subject` is mapped to FHIR R4 element `DetectedIssue.patient` as `SourceIsBroaderThanTarget`.\nThe standard extension `alternate-reference` has been mapped as the representation of FHIR R5 element `DetectedIssue.subject` with unmapped reference targets: BiologicallyDerivedProduct, Device, Group, Location, Medication, NutritionProduct, Organization, Practitioner, Procedure, Substance."
            },
            {
              "code": "DetectedIssue",
              "display": "DetectedIssue",
              "equivalence": "wider",
              "comment": "Element `DetectedIssue.subject` is mapped to FHIR R4 element `DetectedIssue.patient` as `SourceIsBroaderThanTarget`.\nThe standard extension `alternate-reference` has been mapped as the representation of FHIR R5 element `DetectedIssue.subject` with unmapped reference targets: BiologicallyDerivedProduct, Device, Group, Location, Medication, NutritionProduct, Organization, Practitioner, Procedure, Substance."
            }
          ]
        },
        {
          "code": "DetectedIssue.identified[x]",
          "display": "identified[x]",
          "target": [
            {
              "code": "DetectedIssue.identified[x]",
              "display": "DetectedIssue.identified[x]",
              "equivalence": "equivalent",
              "comment": "Element `DetectedIssue.identified[x]` is mapped to FHIR R4 element `DetectedIssue.identified[x]` as `Equivalent`.\nThe target context `DetectedIssue.identified[x]` is a choice-type element and cannot directly hold extensions. The context is moved up to parent element `DetectedIssue`."
            },
            {
              "code": "DetectedIssue",
              "display": "DetectedIssue",
              "equivalence": "equivalent",
              "comment": "Element `DetectedIssue.identified[x]` is mapped to FHIR R4 element `DetectedIssue.identified[x]` as `Equivalent`.\nThe target context `DetectedIssue.identified[x]` is a choice-type element and cannot directly hold extensions. The context is moved up to parent element `DetectedIssue`."
            }
          ]
        },
        {
          "code": "DetectedIssue.author",
          "display": "author",
          "target": [
            {
              "code": "DetectedIssue.author",
              "display": "DetectedIssue.author",
              "equivalence": "wider",
              "comment": "Element `DetectedIssue.author` is mapped to FHIR R4 element `DetectedIssue.author` as `SourceIsBroaderThanTarget`.\nThe standard extension `alternate-reference` has been mapped as the representation of FHIR R5 element `DetectedIssue.author` with unmapped reference targets: Patient, RelatedPerson."
            },
            {
              "code": "DetectedIssue",
              "display": "DetectedIssue",
              "equivalence": "wider",
              "comment": "Element `DetectedIssue.author` is mapped to FHIR R4 element `DetectedIssue.author` as `SourceIsBroaderThanTarget`.\nThe standard extension `alternate-reference` has been mapped as the representation of FHIR R5 element `DetectedIssue.author` with unmapped reference targets: Patient, RelatedPerson."
            }
          ]
        },
        {
          "code": "DetectedIssue.implicated",
          "display": "implicated",
          "target": [
            {
              "code": "DetectedIssue.implicated",
              "display": "DetectedIssue.implicated",
              "equivalence": "equivalent",
              "comment": "Element `DetectedIssue.implicated` is mapped to FHIR R4 element `DetectedIssue.implicated` as `Equivalent`.\nThere's an implicit constraint on the number of implicated resources based on DetectedIssue.type; e.g. For drug-drug, there would be more than one.  For timing, there would typically only be one."
            },
            {
              "code": "DetectedIssue",
              "display": "DetectedIssue",
              "equivalence": "equivalent",
              "comment": "Element `DetectedIssue.implicated` is mapped to FHIR R4 element `DetectedIssue.implicated` as `Equivalent`.\nThere's an implicit constraint on the number of implicated resources based on DetectedIssue.type; e.g. For drug-drug, there would be more than one.  For timing, there would typically only be one."
            }
          ]
        },
        {
          "code": "DetectedIssue.evidence",
          "display": "evidence",
          "target": [
            {
              "code": "DetectedIssue.evidence",
              "display": "DetectedIssue.evidence",
              "equivalence": "wider",
              "comment": "Element `DetectedIssue.evidence` is mapped to FHIR R4 element `DetectedIssue.evidence` as `Equivalent`."
            },
            {
              "code": "DetectedIssue",
              "display": "DetectedIssue",
              "equivalence": "wider",
              "comment": "Element `DetectedIssue.evidence` is mapped to FHIR R4 element `DetectedIssue.evidence` as `Equivalent`."
            }
          ]
        },
        {
          "code": "DetectedIssue.evidence.code",
          "display": "code",
          "target": [
            {
              "code": "DetectedIssue.evidence.code",
              "display": "DetectedIssue.evidence.code",
              "equivalence": "equivalent",
              "comment": "Element `DetectedIssue.evidence.code` is mapped to FHIR R4 element `DetectedIssue.evidence.code` as `Equivalent`."
            },
            {
              "code": "DetectedIssue.evidence",
              "display": "DetectedIssue.evidence",
              "equivalence": "equivalent",
              "comment": "Element `DetectedIssue.evidence.code` is mapped to FHIR R4 element `DetectedIssue.evidence.code` as `Equivalent`."
            }
          ]
        },
        {
          "code": "DetectedIssue.evidence.detail",
          "display": "detail",
          "target": [
            {
              "code": "DetectedIssue.evidence.detail",
              "display": "DetectedIssue.evidence.detail",
              "equivalence": "equivalent",
              "comment": "Element `DetectedIssue.evidence.detail` is mapped to FHIR R4 element `DetectedIssue.evidence.detail` as `Equivalent`."
            },
            {
              "code": "DetectedIssue.evidence",
              "display": "DetectedIssue.evidence",
              "equivalence": "equivalent",
              "comment": "Element `DetectedIssue.evidence.detail` is mapped to FHIR R4 element `DetectedIssue.evidence.detail` as `Equivalent`."
            }
          ]
        },
        {
          "code": "DetectedIssue.detail",
          "display": "detail",
          "target": [
            {
              "code": "DetectedIssue.detail",
              "display": "DetectedIssue.detail",
              "equivalence": "equivalent",
              "comment": "Element `DetectedIssue.detail` is mapped to FHIR R4 element `DetectedIssue.detail` as `Equivalent`.\nShould focus on information not covered elsewhere as discrete data - no need to duplicate the narrative."
            },
            {
              "code": "DetectedIssue",
              "display": "DetectedIssue",
              "equivalence": "equivalent",
              "comment": "Element `DetectedIssue.detail` is mapped to FHIR R4 element `DetectedIssue.detail` as `Equivalent`.\nShould focus on information not covered elsewhere as discrete data - no need to duplicate the narrative."
            }
          ]
        },
        {
          "code": "DetectedIssue.reference",
          "display": "reference",
          "target": [
            {
              "code": "DetectedIssue.reference",
              "display": "DetectedIssue.reference",
              "equivalence": "equivalent",
              "comment": "Element `DetectedIssue.reference` is mapped to FHIR R4 element `DetectedIssue.reference` as `Equivalent`."
            },
            {
              "code": "DetectedIssue",
              "display": "DetectedIssue",
              "equivalence": "equivalent",
              "comment": "Element `DetectedIssue.reference` is mapped to FHIR R4 element `DetectedIssue.reference` as `Equivalent`."
            }
          ]
        },
        {
          "code": "DetectedIssue.mitigation",
          "display": "mitigation",
          "target": [
            {
              "code": "DetectedIssue.mitigation",
              "display": "DetectedIssue.mitigation",
              "equivalence": "wider",
              "comment": "Element `DetectedIssue.mitigation` is mapped to FHIR R4 element `DetectedIssue.mitigation` as `Equivalent`."
            },
            {
              "code": "DetectedIssue",
              "display": "DetectedIssue",
              "equivalence": "wider",
              "comment": "Element `DetectedIssue.mitigation` is mapped to FHIR R4 element `DetectedIssue.mitigation` as `Equivalent`."
            }
          ]
        },
        {
          "code": "DetectedIssue.mitigation.action",
          "display": "action",
          "target": [
            {
              "code": "DetectedIssue.mitigation.action",
              "display": "DetectedIssue.mitigation.action",
              "equivalence": "equivalent",
              "comment": "Element `DetectedIssue.mitigation.action` is mapped to FHIR R4 element `DetectedIssue.mitigation.action` as `Equivalent`.\nThe \"text\" component can be used for detail or when no appropriate code exists."
            },
            {
              "code": "DetectedIssue.mitigation",
              "display": "DetectedIssue.mitigation",
              "equivalence": "equivalent",
              "comment": "Element `DetectedIssue.mitigation.action` is mapped to FHIR R4 element `DetectedIssue.mitigation.action` as `Equivalent`.\nThe \"text\" component can be used for detail or when no appropriate code exists."
            }
          ]
        },
        {
          "code": "DetectedIssue.mitigation.date",
          "display": "date",
          "target": [
            {
              "code": "DetectedIssue.mitigation.date",
              "display": "DetectedIssue.mitigation.date",
              "equivalence": "equivalent",
              "comment": "Element `DetectedIssue.mitigation.date` is mapped to FHIR R4 element `DetectedIssue.mitigation.date` as `Equivalent`.\nThis might not be the same as when the mitigating step was actually taken."
            },
            {
              "code": "DetectedIssue.mitigation",
              "display": "DetectedIssue.mitigation",
              "equivalence": "equivalent",
              "comment": "Element `DetectedIssue.mitigation.date` is mapped to FHIR R4 element `DetectedIssue.mitigation.date` as `Equivalent`.\nThis might not be the same as when the mitigating step was actually taken."
            }
          ]
        },
        {
          "code": "DetectedIssue.mitigation.author",
          "display": "author",
          "target": [
            {
              "code": "DetectedIssue.mitigation.author",
              "display": "DetectedIssue.mitigation.author",
              "equivalence": "equivalent",
              "comment": "Element `DetectedIssue.mitigation.author` is mapped to FHIR R4 element `DetectedIssue.mitigation.author` as `Equivalent`."
            },
            {
              "code": "DetectedIssue.mitigation",
              "display": "DetectedIssue.mitigation",
              "equivalence": "equivalent",
              "comment": "Element `DetectedIssue.mitigation.author` is mapped to FHIR R4 element `DetectedIssue.mitigation.author` as `Equivalent`."
            }
          ]
        }
      ]
    },
    {
      "source": "http://hl7.org/fhir/StructureDefinition/DetectedIssue",
      "sourceVersion": "5.0.0",
      "target": "http://hl7.org/fhir/5.0/StructureDefinition/extension-DetectedIssue.status",
      "targetVersion": "0.1.1",
      "element": [
        {
          "code": "DetectedIssue.status",
          "display": "status",
          "target": [
            {
              "code": "Extension",
              "equivalence": "equivalent",
              "comment": "Element `DetectedIssue.status` is mapped to FHIR R4 element `DetectedIssue.status` as `RelatedTo`.\nThe mappings for `DetectedIssue.status` do not allow expression of the necessary codes, per the bindings on the source and target.\nThe target context `DetectedIssue.status` is a modifier element, so this extension does not need to be defined as a modifier.\nThis element is labeled as a modifier because the status contains the code entered-in-error that marks the issue as not currently valid."
            }
          ]
        }
      ]
    },
    {
      "source": "http://hl7.org/fhir/StructureDefinition/DetectedIssue",
      "sourceVersion": "5.0.0",
      "target": "http://hl7.org/fhir/5.0/StructureDefinition/extension-DetectedIssue.category",
      "targetVersion": "0.1.1",
      "element": [
        {
          "code": "DetectedIssue.category",
          "display": "category",
          "target": [
            {
              "code": "Extension",
              "equivalence": "wider",
              "comment": "Element `DetectedIssue.category` has a context of DetectedIssue based on following the parent source element upwards and mapping to `DetectedIssue`.\nElement `DetectedIssue.category` has no mapping targets in FHIR R4. Typically, this is because the element has been added (is a new element).\nIn addition to the required category valueset, this element allows various categorization schemes based on the owner’s definition of the category and effectively multiple categories can be used at once.  The level of granularity is defined by the category concepts in the value set."
            }
          ]
        }
      ]
    },
    {
      "source": "http://hl7.org/fhir/StructureDefinition/DetectedIssue",
      "sourceVersion": "5.0.0",
      "target": "http://hl7.org/fhir/5.0/StructureDefinition/extension-DetectedIssue.encounter",
      "targetVersion": "0.1.1",
      "element": [
        {
          "code": "DetectedIssue.encounter",
          "display": "encounter",
          "target": [
            {
              "code": "Extension",
              "equivalence": "wider",
              "comment": "Element `DetectedIssue.encounter` has a context of DetectedIssue based on following the parent source element upwards and mapping to `DetectedIssue`.\nElement `DetectedIssue.encounter` has no mapping targets in FHIR R4. Typically, this is because the element has been added (is a new element).\nThis will typically be the encounter the DetectedIssue was created during, but some DetectedIssues may be initiated prior to or after the official completion of an encounter but still be tied to the context of the encounter (e.g. pre-admission lab tests)."
            }
          ]
        }
      ]
    },
    {
      "source": "http://hl7.org/fhir/StructureDefinition/DetectedIssue",
      "sourceVersion": "5.0.0",
      "target": "http://hl7.org/fhir/5.0/StructureDefinition/extension-DetectedIssue.mitigation.note",
      "targetVersion": "0.1.1",
      "element": [
        {
          "code": "DetectedIssue.mitigation.note",
          "display": "note",
          "target": [
            {
              "code": "Extension",
              "equivalence": "wider",
              "comment": "Element `DetectedIssue.mitigation.note` has a context of DetectedIssue.mitigation based on following the parent source element upwards and mapping to `DetectedIssue`.\nElement `DetectedIssue.mitigation.note` has no mapping targets in FHIR R4. Typically, this is because the element has been added (is a new element)."
            }
          ]
        }
      ]
    }
  ]
}